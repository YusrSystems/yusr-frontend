{
  "name": "yusr-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build:core": "pnpm --filter yusr-core build",
    "build:ui": "pnpm --filter yusr-ui build",
    "build": "pnpm build:core && pnpm build:ui"
  }
}